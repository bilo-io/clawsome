# This is a golang API which is essentially just an asset server

Endpoint(s):

- GET /api/v1/marketplace?type=skill
- GET /api/v1/marketplace?type=integration
- GET /api/v1/marketplace?type=plugin

Which return a hard-coded json array from a json file.